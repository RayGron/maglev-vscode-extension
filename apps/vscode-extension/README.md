# Maglev VS Code Extension

Local-first AI coding extension for VS Code.

This extension is bundled from `src/extension.ts` into `dist/extension.js` and executes all filesystem, shell, git, SSH, and deploy actions from the user's machine.
